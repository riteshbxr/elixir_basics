defmodule ElixirBasics.Lessons.InProgress do


  @moduledoc """
  00_in_progres.ex — your personal scratchpad

  Use this file while working through any lesson:
    1. Copy the lesson file here:  cp lib/lessons/<No>_lesson.ex lib/lessons/00_in_progres.ex
    2. Delete everything inside the module body (keep only the shell below)
    3. Open the original lesson side-by-side as a reference
    4. Type each snippet here, then run it to see what happens

  Run this file:
    mix run -e "ElixirBasics.Lessons.InProgress.run()"

  or with either of the below commands:
    mix run inprogress
    mix start

  Or drop into an interactive shell with everything loaded:
    iex -S mix
    iex> ElixirBasics.Lessons.InProgress.run()
  """

  def run do
    # Type your lesson code here — delete this comment when you start
    IO.puts("Ready. Start typing your lesson snippets here.")
  end

end
